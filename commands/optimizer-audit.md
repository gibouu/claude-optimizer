---
description: Audit claude-optimizer compliance for the current session — edits, commits, state writes, harness firings, and a derived compliance percentage. Read-only.
---

Run `${CLAUDE_PLUGIN_ROOT}/scripts/optimizer_audit.sh` and present its output verbatim.

The script is purely read-only. Do not modify any state file as part of the audit. Do not add commentary or interpretation — the script's output is already formatted for the user.

If the user asks for explanation after seeing the output, you can interpret it then.
