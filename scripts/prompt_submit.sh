#!/usr/bin/env bash
# Currently a no-op placeholder. Reserved for future redaction / prompt-shaping logic.
# Reads the user prompt on stdin (Claude Code sends event JSON here).
# Exiting 0 with no output = pass-through.
exit 0
